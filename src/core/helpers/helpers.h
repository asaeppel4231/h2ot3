#ifndef HELPERS_H
#define HELPERS_H

//#include <stdlib.h>

/* first let us comment this
#define RETURN_IF_NULL(ptr, value) \
do{\
    if((ptr) == NULL){ \
        return (value); \
    } \
}while(0)*/

#define DEBUG "[ DEBUG ]: "

#endif